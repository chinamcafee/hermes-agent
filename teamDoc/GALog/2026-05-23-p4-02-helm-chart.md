# P4-02 Helm chart 工作日志

## 背景

- 工作包：`P4-02 | Helm chart | values、secrets、ingress、persistence、jobs | P4-01 | 3`
- 当前阶段：P4 Beta 验证，P4-01 Compose hardening 已完成。
- 设计依据：
  - `teamDoc/GAStep/05-phase-4-5-beta-ga-steps.md` 的部署硬化要求。
  - P4-01 已固定 Compose healthcheck、backup mount 和 dev TLS mount。

## 执行计划

1. 用 Helm chart 静态 contract 测试定义 chart、values、secret、ingress、persistence 和 job 要求。
2. 在 `deploy/team-cloud/helm/hermes-team-cloud/` 创建 chart 工件。
3. 将 P4-02 测试纳入 foundation smoke 矩阵。
4. 运行 P4-02 单测、部署回归、ruff、py_compile、JSON/YAML 校验和 diff 检查。

## 实时记录

- 2026-05-23：P4-02 标记为 In Progress，开始按 TDD 红绿循环推进。
- 2026-05-23：红灯验证已执行，`scripts/run_tests.sh tests/team_cloud/test_helm_chart.py` 出现 3 个预期失败，分别指向 chart metadata/values、templates、文档和 smoke 注册缺口。
- 2026-05-23：新增 `deploy/team-cloud/helm/hermes-team-cloud/` chart，包括 `values.yaml`、Secret/ConfigMap/PVC、API/worker/web workloads、services、ingress 和 migration/SpiceDB schema/MinIO bucket jobs；新增 P4-02 文档并注册 foundation smoke。
- 2026-05-23：绿灯验证通过，`scripts/run_tests.sh tests/team_cloud/test_helm_chart.py` 结果为 1 个文件、3 个测试通过。
- 2026-05-23：相关回归通过，`scripts/run_tests.sh tests/team_cloud/test_helm_chart.py tests/team_cloud/test_compose_hardening.py tests/team_cloud/test_platform_foundation_suite.py` 结果为 3 个文件、9 个测试通过。
- 2026-05-23：静态验证通过，`venv/bin/ruff check ...`、`venv/bin/python -m py_compile ...`、`venv/bin/python -m json.tool teamDoc/GADoc/artifacts/platform-foundation-smoke-v0.json >/dev/null` 和 `git diff --check ...` 均为 exit 0；本机未安装 `helm`，未执行 `helm lint`。
- 2026-05-23：完整 foundation smoke 通过，`scripts/team-cloud-foundation-smoke.sh` 结果为 70 个文件、246 个测试通过。
